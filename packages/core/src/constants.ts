/**
 * API version included in all JSON responses.
 * Increment on breaking schema changes.
 */
export const API_VERSION = "1";

/**
 * Compatibility constant for the wiki root. Wikiplane stores raw/, wiki/, and AGENTS.md directly at the brain repository root.
 */
export const WIKI_DIR_NAME = ".";
